import
    tother

type Vec = object
    x: int
    y: int

proc `$`(x: Vec): string = $x & ":" & $y
proc print(x: Vec) = echo x
proc sum(a, b: Vec): Vec = Vec(
    x: a.x + b.x,
    y: a.y + b.y)

var a: Vec
var b: Vec
var z: X

a.sum(

asdoifjasdoi # Works after error
var x: Vec
b.
